server:
  port: 8084

spring:
  application:
    name: cart-service

  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://JananiSivasankar:Janani2407@cluster0.rqcvycq.mongodb.net/ecommerceDB?retryWrites=true&w=majority&appName=Cluster0}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

    register-with-eureka: true
    fetch-registry: true

logging:
  level:
    com.example.cartservice: DEBUG
